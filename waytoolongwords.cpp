#include <stdio.h>

int main()
{
    int n;
    char y;

    input word;
    print line n;

    length_w = length.word;
    if(length_w > n && length_w < 100){
        nilai tengah = length_w - 2;
        hasil = str 0 + tengah + str length_w;

    }else{
        hasil = word;
    }


    printf("Hello World cpp vscode");
    return 0;
}
